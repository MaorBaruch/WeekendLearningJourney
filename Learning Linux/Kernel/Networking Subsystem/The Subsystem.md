
## Purpose
The networking subsystem handles everything that has got to do with computer networks (for example, handling of packets).
The networking subsystem is not a mandatory part of the linux kernel, and the kernel can be compiled without it.